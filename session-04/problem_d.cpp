#include <stdio.h>
#include <math.h>

// me math be poopoo

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);

    if (6 * a >= b && b >= a)
    {
        printf("Yes\n");
    }
    else
    {
        printf("No\n");
    }

    return 0;
}